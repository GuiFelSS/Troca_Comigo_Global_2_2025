# --- Banco de Dados (Perfeito, mantenha assim) ---
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/orcl
spring.datasource.username=${DB_USER:rm556834}
spring.datasource.password=${DB_PASS:070301}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# --- JPA / Flyway (Mantenha) ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# --- Email (Perfeito, use as variáveis) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:seuemail@gmail.com}
spring.mail.password=${MAIL_PASS:sua_senha_app}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- JWT (Perfeito) ---
jwt.secret.key=${JWT_SECRET:ZGlkYXRpY28uZXN0ZS5lLnVtLmNvZGlnby5zZWNyZXRvLnBvci50cmFzLmRlLm1pbS4xMjM=}
jwt.expiration.ms=3600000

# ===============================================
# Configuração Spring AI / Vertex AI
# ===============================================
spring.ai.vertex.ai.gemini.project-id=global-solution-skillswap
spring.ai.vertex.ai.gemini.location=us-central1
spring.ai.vertex.ai.gemini.chat.options.model=gemini-1.5-flash

# AQUI ESTÁ O TRUQUE:
# Em vez de apontar para um arquivo físico fixo, apontamos para uma variável.
# Se a variável 'GCP_CREDENTIALS' não existir, ele tenta ler um arquivo local (útil para dev).
spring.ai.vertex.ai.gemini.credentials-uri=${GCP_CREDENTIALS_URI:classpath:gcp-credentials.json}