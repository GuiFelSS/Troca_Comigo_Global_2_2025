#--------------------------------LOCAL (Commented out)-------------------------------
##--- POSTGRESQL (Ativo) ---
##spring.datasource.url=${SPRING_DATASOURCE_URL}
##spring.datasource.username=${DB_USER}
##spring.datasource.password=${DB_PASS}
##spring.datasource.driver-class-name=org.postgresql.Driver
##spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#
##--- H2 (Ativo para desenvolvimento local) ---
#spring.datasource.url=jdbc:h2:mem:skillswapdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=Oracle
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=
#spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
#
## --- Habilitar o H2 Console ---
#spring.h2.console.enabled=true
#spring.h2.console.path=/h2-console
#
## --- Hibernate ---
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#
## --- Flyway (Desabilitado) ---
#spring.flyway.enabled=false
#
## --- Email (Gmail) ---
## Valores padrao "mock" para a aplicacao iniciar.
## Voce DEVE sobrescrever com variaveis de ambiente na sua IDE para funcionar.
#spring.mail.host=smtp.gmail.com
#spring.mail.port=587
#spring.mail.username=${MAIL_USER:seu-email@gmail.com}
#spring.mail.password=${MAIL_PASS:sua-senha-de-app}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#
## --- Mensageria (RabbitMQ) ---
## Define 'localhost' como padrao se a variavel de ambiente nao existir
#spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
#spring.rabbitmq.queue=${RABBIT_QUEUE_NAME:fiap-gs-email-queue}
#
## --- Seguranca (JWT) ---
#jwt.secret.key=${JWT_SECRET:ZGlkYXRpY28uZXN0ZS5lLnVtLmNvZGlnby5zZWNyZXRvLnBvci50cmFzLmRlLm1pbS4xMjM=}
#jwt.expiration.ms=3600000
#
## --- IA Generativa (DeepSeek) ---
## Voce DEVE sobrescrever com sua chave real na IDE para funcionar.
#spring.ai.deepseek.api-key=${DEEPSEEK_API_KEY:sk-0ffcea6dbbd347119f421c57bfde794e}
#spring.ai.deepseek.chat.options.model=deepseek-chat
#
## --- OLLAMA (Local/Desativado) ---
##spring.ai.ollama.base-url=http://localhost:11434
##spring.ai.ollama.chat.options.model=llama3

#--------------------------------PRODUCTION (Render)-------------------------------

#--- POSTGRESQL (Active for Render Deploy) ---
spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#--- H2 (Disabled) ---
#spring.h2.console.enabled=false

# --- Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# --- Flyway (Disabled) ---
spring.flyway.enabled=false

# --- Email (Gmail) ---
# (Values will be injected by Render)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- RabbitMQ ---
# (Values will be injected by Render)
spring.rabbitmq.host=${RABBITMQ_HOST}
spring.rabbitmq.queue=${RABBIT_QUEUE_NAME}

# --- Security (JWT) ---
# (Values will be injected by Render)
jwt.secret.key=${JWT_SECRET}
jwt.expiration.ms=3600000

# --- Generative AI (DeepSeek) ---
# (Values will be injected by Render)
spring.ai.deepseek.api-key=${DEEPSEEK_API_KEY}
spring.ai.deepseek.chat.options.model=deepseek-chat

# --- OLLAMA (Disabled) ---
#spring.ai.ollama.base-url=http://localhost:11434
#spring.ai.ollama.chat.options.model=llama3